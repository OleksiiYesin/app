#!groovy
pipeline {
    agent {
        label "static_slave"
    }
    stages {
        
        stage('cat README') {
            when {
                branch "feature"
            }
            steps {
                sh 'git log --format=%s%b -n 1'
            }
        }
    }
} 
