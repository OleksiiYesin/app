#!groovy
pipeline {
    agent {
        label "dynamic_slave"
    }
    stages {

        stage( ' Example ' ) {
        if  (env.BRANCH_NAME == ' feature ' ) {
            sh 'cat test.txt'
        } else  {
            echo ' This is branch not feature '
        }
        }
    }
}
