#!groovy
pipeline {
    agent {
        label "static_slave"
    }
    stages {
        
        stage('hello') {
            steps {
              echo "Hello!"
            }
        }

        stage('cat README') {
            when {
                branch "feature"
            }
            steps {
                sh 'cat README.md'
            }
        }
    } 
}
