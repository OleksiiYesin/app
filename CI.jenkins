#!groovy
pipeline {
    agent {
        label "static_slave"
    }
    stages {
        stage('hello') {
            steps {
              echo "Hello from MAIN branch!"
            }
        }
    }
}